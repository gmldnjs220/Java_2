# 202230211 문희원

## 5주차 2026.04.01
- 비트 논리 연산 : 비트끼리 and, or, xor, not 연산
- 비트 연산
    - 성능이 중요한 경우나 최적화가 필요한 경우에 많이 활용
        - flag = 변수 하나를 지정해 작업을 하는중이면 0, 완료하면 1로 설정하여 작업을 끝냄
- 조건문 - IF문
    - 단순 if문
    - if - else문
        - else가 있냐, 아니냐 차이
- switch문
    - switch 문의 식과 case 문의 값과 비교
    - case의 비교 값과 일치하면 해당 case의 실행문장 수행
    - break를 만나면 switch문을 벗어남
    - case의 비교 값과 일치하는 것이 없으면 default 문 실행
    - default문은 생략가능
    - break문의 역할
        - break문을 만나면 switch문 벗어남
        - case 문에 break문이 없다면, 다음 case문 계속 실행
    - 실수는 조건으로 사용 안됌
- 배열
    - 인덱스와 인덱스에 대응하는 데이터들로 이루어진 자료 구조로 한 번에 많은 메모리 공간 선언.
    - 같은 타입의 데이터들이 순차적으로 저장되는 공간으로 인덱스를 이용하여 원소 데이터 접근
    - 반복문을 이용하여 처리하기에 적합한 자료 구조
    - 배열 인덱스 : 0부터 시작

## 4주차 2026.03.25
- 가비지 컬렉션
    - 자바 언어는 메모리 할당 기능은 있어도 메모리 반환 기능 없음
    - 사용하지 않는 메모리는 jvm에 의해 자동 반환 → 가비지 컬렉션
    - 메모리를 자동으로 청소해 주는 역할을 함
- 자바 특징
    - 실시간 응용프로그램에 부적합
        - 실행 도중 예측할 수 없는 시점에 가비지 컬렉션 실행 때문
        - 응용프로그램의 일시적 중단 발생
- 자바 프로그램은 안전한 부분
    - 타입 체크 엄격
    - 물리적 주소 ??? 를 사용하는 포인터 개념 없음
- 프로그램 작성 쉬움
    - 포인터 개념이 없음. 동적 메모리 반호나 하지 않음. 다양한 라이브러리 지원.
- 실행 속도 개선을 위한 JIT 컴파일러 사용
    - 자바는 바이트 코드를 인터프리터 방식으로 실행
    - 기계어가 실행되는 것보다 느림
    - JIT컴파일 기법으로 실행 속도 개선
    - JIT 컴파일 - 실행 중에 바이트 코드를 기계어 코드로 컴파일 하여 기계어를 실행하는 기법
- 단어
    - 소스코드
        - 우리가 작성하는 코드
        - 사람이 읽을 수 있는 고수준 언어
    - 바이트 코드(bytecode, .class 파일)
        - JAVA 컴파일러가 소스코드를 변환한 중간 코드
        - cpu가 직접 실행할 수 없음 → JVM이 실행해야함
        - 기계어와 다르게 플랫폼 독립적
    - 기계어
        - cpu가 직접 실행할 수 있는 0과 1의 이진 코드
        - 운영체제와 cpu 아키텍처(Intel, arm 등)에 따라 다름
        - 16진수 형태의 기계어
- 식별자
    - 클래스, 변수, 상수, 메소드 등에 붙어있는 이름
- 리터럴 = 소스 코드 중 특정한 자료형의 값을 의미
    - JDK, 한글, 계속하세요
- 참조 자료형
    - 포인터는 임의의 메모리 주소를 저장 하는 반면, 참조 자료형은 주소를 저장할 수 없음
    - 직접 주소를 갖고 있지는 않지만, JVM이 해당 주소로 안내해 주게 됩니다.
    - 객체를 참조하는 변수 유형으로 , 힙 영역에 저장된 객체의 메모리 주소를 가리킵니다.
        - 기본 자료형은 스택영역에 저장됨
    - new 키워드로 객체를 생성하는 것이 바로 참조 자료형을 선언하는 것입니다.
    - 이렇게 선언된 참조 자료형은 JVM이 대신 객체의 주소를 저장합니다.
    - 배열, 인터페이스 혹은 열거형도 객체이기 때문에 참조 자료형 입니다.
    - 객체를 참조하지않을때 null 값을 가질 수 있음
- 상수 선언은 앞에 final을 붙여 줌 EX)final int PI = 1;
또한 상수 선언은 모든 알파벳을 대문자로 이름을 붙여야함
- var 키워드
    - var 키워드는 타입을 생략하고 변수 선언을 할 수 있음
    - 컴파일러가 추론하여 변수 타입을 결정함
    - 변수 선언할 때 초기값을 주지 않으면 컴파일 오류가 발생함
    - var는 지역 변수 선언에만 사용이 가능하고 클레스 필드에서는 사용할 수 없음
        - 지역변수 : 메소드 내부에 선언되는 변수
        - 클래스 필드 : 클래스 내부에 선언되는 변수.. 객체가 생성될 때 함께 만들어지는 변수
- scanner 클래스와 system.in이 있는데 대부분 scanner 클래스를 사용함
- scanner 클래스
    - 읽은 바이트를 문자, 정수, 실수, 불린, 문자열 등 다양한 타입으로 변환하여 리턴.
        - java.utill.Scanner
    - 객체를 생성해서 사용
    - 키보드에 연결된 [System.in](http://System.in)에게 키를 읽게 하고, 원하는 타입으로 변환하여 리턴
    - 입력되는 키 값을 공백으로 구분되는 토큰 단위로 읽음
    - 공백 문자 : \t, \r, \n, ‘’, \f = 페이지 나누기, 폼피드, 프린트에서 사용


## 3주차 2026.03.18
- ctrl + shift + p → 자바 프로젝트 생성
- pascal case - 클래스나 파일모두 첫글자는 대문자 → 자바
- camale case - 두번째 단어부터 대문자
- 프로그래밍 언어
    - 기계어
        - 0과1의 이진수로 구성된 언어
        - 컴퓨터의 cpu는 기계어만 이해하고 처리가 가능함
    - 어셈블리어
        - 기계어가 명령을 add, sub, move 등과 같은 표현하기 쉬운 상징적인 단어인 니모닉 기호로 일대일 대응 시킨 언어
        - 하드웨어에서 주로 쓰임
    - 고급언어
        - 사람이 사용하는 언어로 이해하기 쉽고 쉽게 표현할 수 있음
        - ex) pascal,  java, c+
- 프로그램 언어
    - 절차 지향 언어
        - 절차, 순서에 따라서 실행하는 방식
        - 데이터와 함수를 분리하여 작성
        - 코드의 유연성 부족, 재 사용이 어려움
        - 가독성과 유지 보수가 어려움
        - ex)c, pascal, fortran
    - 객체 지향 언어
        - 현실의 객체를 모델링하여 프로그램을 작성하는 방식
        - 객체는 데이터와 데이터를 처리하는 메소드(함수) 모두 포함
        - 상속, 캡슐화, 다향성 등의 개념을 활용하여 유연하고 재사용 가능한 코드를 작성할 수 있음
        - ex) Java, C++, Python
    - 함수 지향언어
        - 함수형 언어는 함수를 일급 객체로 취급하고, 상태 변경을 피하고 불변성을 지향하는 프로그래밍 패러다임
        - 함수형 언어에서는  함수 조합으로 복잡한 작업을 수행하며, 상태 변경 대신에 데이터를 변환하는 방식으로 프로그램을 작성함
        - 재귀, 고차함수, 순수 함수 등의 개념을 중요시하며, 병렬 처리와 높은 수준의 추상화를 지원
        - 함수 지향 언어는 객체 지향 언어의 특성을 그대로 가져가되, 함수 지향적인 기능을 추가한 것
        - ex) Python, Kotlin, Haskell, Lisp, Scala
        - 객체 지향 언어의 복잡함을 보완하기 위해 나옴

![img.png](attachment:539faa50-fb93-4a29-955d-4dc03017d457:img.png)

- 프로그래밍과 컴파일
    - 소스 : 프로그래밍 언어로 작성된 텍스트 파일
    - 컴파일 : 소스 파일을 컴퓨터가 이해할 수 있는 기계어로 만드는 과정
    - 자바 : .java → .class
    - c : .c → .obj → .exe
    - c++ : .cpp → .obj → .exe
- **JAVA**
    - 95년도 출시
    - 목적
        - 플랫폼 간의 호환성 문제해결
        - 플랫폼 독립적인 언어 개발
        - 메모리 사용량이 적고 다양한 플랫폼에 적용
            - 가전제품
    - JVM(java virture machine)
        - 자바 가상머신
        - os 별로 컴파일을 각각해야 하는 문제를 jvm 을 그 형식에만 맞게 입력하면 쉽게 os 별로 컴파일 할 수 있음
        - 바이트 코드(.class)
            - JVM 에서 실행 가능한 바이너리 코드
            - 바이트 코드는 컴퓨터 cpu에 의해 직접 실행되지 않음
            - JVM이 작동 중인 플랫폼에서 실행
            - 인터프리터 방식으로 바이트 코드 해석
    - 실행 환경
        - JVM + JAVA PAI(클래스 라이브러리)로 구성
            
            ![1_CjisOp3Z4RsADCBGRlSTFQ.png](attachment:7ea4d6db-c458-4ac2-bff1-770a34f322c4:1_CjisOp3Z4RsADCBGRlSTFQ.png)
            
    - JAVA 표준안
        - JAVA SE
            - 표준 자바 플랫폼
        - JAVA EE
            - 서버개발을 위한 플랫폼
        - JAVA ME
            - 임베디드를 위한 자바 플랫폼
    - 자바 특성
        - 플랫폼 독립성
            - 하드웨어, 운영체제에 종속되지 않는 바이트 코드로 플랫폼 독립성
        - 객체지향 : 캡슐화, 상속, 다향성 지원
        - 클래스로 캡슐화
    - 가비지 컬렉션
- 플랫폼 = 하드웨어 플랫폼 + 운영체제 플랫폼
- 프로그램의 플랫폼 호환성이 없는이유
    - 기계어가 cpu마다 다름
    - 운영체제가 api 다름
    - 실행파일 형식이 다름
- JDK
    - 자바 개발 킷
- JRE
    - 실행시키는 파일
    - JDK 안에 JRE가 있음
- 버전관리
    - 첫 번째 자리 → 대대적인 변화
    - 두 번째 자리 → 기능 추가
    - 세 번째 자리 → 오류 수정
    - EX) 3.16.5
- 터미널에서 class 생성 방법
    - javac ./src/프로젝트 이름.java

## 2주차 2026.03.11
- branch = 가지
    - 중급정도에 이해해도됌
- master = 굵은 뼈대
    - git과 vscode 연결하기 위해 필요
- source control
    - =git
- 커밋
    - 메모
- master/main
    - git - master
    - github - main
    - vscode - main

## 1주차 2026.03.04
- Repositroy(저장소)
    - 파일을 관리하기 위한 단위 = 폴더
- Remote repository(서버 저장소)
- Branch(브랜치, 가지)
    - 독립적인 작업 공간을 제공하여 여러 개발자들이 동시에, 또는 특정 기능이나 버그수정과 같이 서로 다른 작업을 병렬적으로 진행하도록 돕는 기능
- Default Branch(기본 브랜치, 기둥)
    - =master
    - =Main

---

Git 설치 방법

1. Git 접속
2. Install window 클릭
3. Git for windows /x64 set 클릭
4. 실행 후 5번째 페이지에서 main으로 선택 후 설치

---

- 깃 사전
    - Pro git

---

깃 명령어

```jsx
#사용자 등록
git config --global user.name "Your name"
git config --global user.email "your-email@example.com"

git init #새로운 git 저장소 만들기
git add #변경된 모든 파일을 staging
git commit -m"설명" #commit 하기
git log --oneline #commit 기록 확인
```
